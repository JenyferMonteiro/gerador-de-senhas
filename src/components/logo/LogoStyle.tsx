import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  logo: {
    resizeMode: 'contain',
    height: 90,
    width: 90,
  },
  titulo: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 25,
    color: 'red',
  }
});